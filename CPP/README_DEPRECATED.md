# Deprecated

This folder contains older external C++/export experiments kept for reference.

The active bit-true deployment reference chain is now under:

- `refactor/bittrue`

If you need the current export/runtime path, use:

- `refactor/export`
- `refactor/bittrue/cpp`

Do not treat this folder as the main implementation path.
