# Deprecated

This directory is now a frozen legacy mirror of the older C++ reference runtime experiments.

Active development has moved to:

- `refactor/bittrue/cpp`

Do not add new functionality here.

If an old script still points to this directory, migrate it to the new bit-true path instead of extending the legacy files.
